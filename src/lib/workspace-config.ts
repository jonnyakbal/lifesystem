/** Public presentation settings. Hiding a module does not disable its API or delete data. */
export const workspaceConfig = {
  name: 'LIFESYSTEM',
  tagline: 'Seu universo, em movimento',
  // Add route paths here to hide optional destinations from navigation.
  hiddenModules: [] as string[],
};
